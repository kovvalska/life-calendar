# Skopiuj do .env i uzupełnij. Plik .env nie powinien być w repozytorium.

# Baza – connection string z MongoDB Atlas (wymagane w produkcji)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/life-calendar?retryWrites=true&w=majority

# Sekret do JWT (wymagane w produkcji). Wygeneruj: openssl rand -base64 32
JWT_SECRET=twoj-dlugi-losowy-ciag

# Opcjonalne: tryb (production przy deploy)
NODE_ENV=development

# === Email – wybierz JEDNĄ metodę (API działa na Render; SMTP często: Connection timeout) ===
#
# OPCJA 1 – SendGrid (zalecane, bez domeny: zweryfikuj Gmail w SendGrid → Single Sender):
# SENDGRID_API_KEY=SG.xxx...
# SENDGRID_FROM="Kalendarz Życia" <twoj-zweryfikowany@gmail.com>
#
# OPCJA 2 – Brevo (300/dzień): BREVO_API_KEY=xxx, BREVO_FROM="Kalendarz Życia" <zweryfikowany@email.com>
# OPCJA 3 – Resend (wymaga domeny): RESEND_API_KEY=re_xxx, RESEND_FROM="..." <noreply@domena.pl>
#
# OPCJA 4 – SMTP (Gmail/Outlook; na Render często timeout):
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=twoj@gmail.com
# EMAIL_PASS=haslo-aplikacji-gmail
# EMAIL_FROM="Kalendarz Życia" <twoj@gmail.com>
